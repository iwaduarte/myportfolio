{
  "name": "myportfolio-backend",
  "version": "1.0.0",
  "description": "lambda designed to send email via smtp aws",
  "main": "app.js",
  "scripts": {
    "test": "jest",
    "deploy": "serverless deploy -v"
  },
  "author": "@iwaduarte",
  "license": "ISC",
  "devDependencies": {
    "jest": "^25.2.4"
  },
  "dependencies": {
    "aws-sdk": "^2.814.0"
  }
}
